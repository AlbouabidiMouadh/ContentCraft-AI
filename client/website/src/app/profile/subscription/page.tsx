import ProfileContainer from '@/containers/ProfileContainer'
import React from 'react'

const index = () => {
  return (
    <ProfileContainer>
      this is the profile subscription page
    </ProfileContainer>
  )
}

export default index