import React from "react";

const Footer = () => {
  return (
    <div style={{ minHeight: "500px", backgroundColor: "black" }}>
      this s the footer component
    </div>
  );
};

export default Footer;
